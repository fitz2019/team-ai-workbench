interface:
  display_name: "Docker Patterns"
  short_description: "Work with containers and local services"
  default_prompt: "Use $docker-patterns to review this container or local orchestration change."
policy:
  allow_implicit_invocation: true
