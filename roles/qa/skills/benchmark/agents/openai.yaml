interface:
  display_name: "Benchmark"
  short_description: "Measure performance baselines and regressions"
  default_prompt: "Use $benchmark to define a small performance baseline and compare this change against it."
policy:
  allow_implicit_invocation: true
