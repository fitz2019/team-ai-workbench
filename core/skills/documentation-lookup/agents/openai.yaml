interface:
  display_name: "Documentation Lookup"
  short_description: "Verify libraries against primary docs"
  default_prompt: "Use $documentation-lookup to verify this library or framework behavior against current primary docs."
dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Context7 MCP server for current library documentation."
policy:
  allow_implicit_invocation: true
