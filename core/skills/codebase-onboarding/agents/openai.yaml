interface:
  display_name: "Codebase Onboarding"
  short_description: "Map an unfamiliar backend codebase"
  default_prompt: "Use $codebase-onboarding to explore this backend codebase and produce a concise onboarding map."
policy:
  allow_implicit_invocation: true
