interface:
  display_name: "Verification Before Completion"
  short_description: "Require evidence before completion claims"
  default_prompt: "Use $verification-before-completion before claiming this work is complete or fixed."
policy:
  allow_implicit_invocation: true
